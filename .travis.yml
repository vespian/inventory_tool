language: python
python:
  - "3.2"
  - "3.3"
  - "3.4"
install: "pip install -r requirements.txt --use-mirrors"
script: "./run_tests.py"
before_install:
  - sudo apt-get update -qq
  - sudo apt-get install -y python3-ipaddr
